# Phase 01: App Shell

## Goal

Create the Expo app using the official default template, move routes under `src/app`, configure Android metadata, EAS, theme, and placeholder navigation.

## Checks

- App starts with Expo Router.
- TypeScript strict mode is enabled.
- Lint command runs.
- No unnecessary Android permissions are declared.

## Status

Complete.
